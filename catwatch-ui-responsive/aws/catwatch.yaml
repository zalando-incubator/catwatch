
# basic information for generating and executing this definition
SenzaInfo:
  StackName: catwatch
  Parameters:
    - ImageVersion:
        Description: "Docker image version of catwatch."

# a list of senza components to apply to the definition
SenzaComponents:

  # this basic configuration is required for the other components
  - Configuration:
      Type: Senza::StupsAutoConfiguration # auto-detect network setup

  # will create a launch configuration and auto scaling group with scaling triggers
  - AppServer:
      Type: Senza::TaupageAutoScalingGroup
      InstanceType: t2.micro
      SecurityGroups:
        - app-catwatch
      IamRoles:
        - app-catwatch
      ElasticLoadBalancer: AppLoadBalancer
      AssociatePublicIpAddress: false # change for standalone deployment in default VPC
      TaupageConfig:
        application_version: "{{Arguments.ImageVersion}}"
        runtime: Docker
        source: "pierone.stups.zalan.do/mentoring/catwatch:{{Arguments.ImageVersion}}"
        health_check_path: /healtchcheck
        ports:
          8080: 8080
        scalyr_account_key: 00__Qhet83Yi/5ZOuLUvMtPaaU_UjJO49vN52/xwGTas- 
        mint_bucket: "zalando-stups-mint-187355903146-eu-west-1"

  # creates an ELB entry and Route53 domains to this ELB
  - AppLoadBalancer:
      Type: Senza::WeightedDnsElasticLoadBalancer
      HTTPPort: 8080
      HealthCheckPath: /healtchcheck
      SecurityGroups:
        - app-catwatch-lb
